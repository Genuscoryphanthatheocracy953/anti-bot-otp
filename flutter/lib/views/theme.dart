import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const brand900 = Color.fromRGBO(15, 23, 42, 1.0);
  static const brand800 = Color.fromRGBO(30, 41, 59, 1.0);
  static const brand500 = Color.fromRGBO(59, 130, 246, 1.0);
  static const brand400 = Color.fromRGBO(96, 165, 250, 1.0);
  static const brand100 = Color.fromRGBO(219, 232, 254, 1.0);

  // Surfaces
  static const surface = Colors.white;
  static const bg = Color.fromRGBO(248, 250, 252, 1.0);

  // Text
  static const textPrimary = Color.fromRGBO(15, 23, 42, 1.0);
  static const textSecondary = Color.fromRGBO(100, 116, 139, 1.0);
  static const textMuted = Color.fromRGBO(148, 163, 184, 1.0);

  // States
  static const success = Color.fromRGBO(16, 185, 129, 1.0);
  static const error = Color.fromRGBO(239, 68, 68, 1.0);
  static const border = Color.fromRGBO(226, 232, 240, 1.0);

  // Defense card
  static const defenseBg = Color.fromRGBO(240, 249, 255, 1.0);
  static const defenseBorder = Color.fromRGBO(186, 230, 253, 1.0);
  static const defenseIcon = Color.fromRGBO(14, 165, 233, 1.0);
}

class AppSpacing {
  static const double cardRadius = 16;
  static const double cardPadding = 24;
  static const double inputRadius = 12;
  static const double buttonRadius = 12;
  static const double buttonHeight = 50;
  static const double sectionGap = 24;
  static const double innerGap = 16;
}
