name: otp_poc
description: OTP Auth PoC - Cross-platform Flutter client with 11 security defense layers.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  crypto: ^3.0.6
  pointycastle: ^3.9.1
  asn1lib: ^1.5.8
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
